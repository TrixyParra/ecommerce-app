export const cartReducer = (state, action) => {
    // check for the action + or - 
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state; 
    } 
};  